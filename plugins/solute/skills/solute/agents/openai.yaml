interface:
  display_name: "Solute"
  short_description: "Sol-led orchestration with Luna workers"
  default_prompt: "Use $solute to execute this task with Luna workers."

policy:
  allow_implicit_invocation: false
